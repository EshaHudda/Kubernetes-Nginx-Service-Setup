kind: ResourceQuota
apiVersion: v1
metadata:
  name: pod-resource-quota
  namespace: nginx
spec:
  hard:
    pods: "5"
    requests.cpu: "2"
    requests.memory: "4Gi"
    limits.cpu: "4"
    limits.memory: "8Gi" 